
# .github/workflows/workflow.yml
name: CI workflow

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    container: python:3.9-slim

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Upgrade build tooling
        run: python -m pip install --upgrade pip setuptools wheel

      - name: Install dependencies (verbose to locate failing package)
        run: |
          pip install -r requirements.txt -vv

      - name: Install flake8
        run: pip install flake8

      - name: Lint with flake8
        run: |
          flake8 $(params.path) --count --ignore=E201,E202,E203,E231 --max-line-length=127 --statistics
          flake8 service --count --max-complexity=10 --max-line-length=127 --statistics

      - name: Run unit tests with nose
        run: |
          pip install nose coverage nose-spec
          nosetests -v --with-spec --spec-color --with-coverage --cover-package=app
